<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.josejoescobar.medlinenetwork2">

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="com.google.android.providers.gsf.permisson.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/iconoapp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logomedico"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Registro" />
        <activity android:name=".MenuUsuario" />
        <activity android:name=".DoctoresUsuario" />
        <activity android:name=".HistorialMedico" />
        <activity android:name=".Medicinas" />
        <activity android:name=".SolicitarMedicinas" />
        <activity android:name=".VerPacientesDoctor" />
        <activity android:name=".CitasDoctor" />
        <activity android:name=".Agregar" />
        <activity android:name=".Ayuda" />
        <activity android:name=".Buscar" />
        <activity android:name=".Citas" />
        <activity android:name=".Escoger" />
        <activity android:name=".NuevoPaciente" />
        <activity android:name=".PrescribirMedicinas" />
        <activity android:name=".Login" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        android:label="@string/title_activity_maps"/>
        <activity android:name=".LaAyuda"></activity>
    </application>

</manifest>